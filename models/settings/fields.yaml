# ===================================
#  Field Definitions
# ===================================

tabs:
  fields:

    default_payment_page:
      label: Default payment page
      tab: Payments
      type: dropdown
      comment: Used for creating payment links from the backend and invoice templates.
