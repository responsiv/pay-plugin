# ===================================
#  List Column Definitions
# ===================================

columns:
    description:
        label: Description

    price:
        label: Price
        type: currency
        currencyFrom: invoice.currency_code

    discount:
        label: Discount
        type: currency
        currencyFrom: invoice.currency_code

    quantity:
        label: Quantity
        shortLabel: Qty
        type: number

    subtotal:
        label: Total
        type: currency
        currencyFrom: invoice.currency_code
