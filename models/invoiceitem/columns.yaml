# ===================================
#  List Column Definitions
# ===================================

columns:
    description:
        label: Description

    quantity:
        label: Qty

    price:
        label: Unit Price
        type: currency

    discount:
        label: Discount
        type: currency

    total:
        label: Amount
        type: currency
