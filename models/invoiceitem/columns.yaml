# ===================================
#  List Column Definitions
# ===================================

columns:
    description:
        label: Description

    price:
        label: Price
        type: currency

    discount:
        label: Discount
        type: currency

    quantity:
        label: Quantity
        shortLabel: Qty
        type: number

    subtotal:
        label: Total
        type: currency
