# ===================================
#  Field Definitions
# ===================================

tabs:
    fields:

        invoice_prefix:
            label: Invoice prefix
            comment: Enter a value to prefix all invoice numbers.
            tab: Invoices
            span: auto

        new_invoice_number:
            label: New invoice number
            comment: Use this to set the next invoice number, must be more than the current invoice number.
            tab: Invoices
            span: auto
