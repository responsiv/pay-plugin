# ===================================
#  List Column Definitions
# ===================================

columns:
    payment_type_name:
        label: Gateway

    message:
        label: Message

    created_at:
        label: Recorded
        type: datetime
