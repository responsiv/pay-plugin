# ===================================
#  List Column Definitions
# ===================================

columns:
    credit_note_id:
        label: Credit Note
        type: number
        width: 120px

    amount:
        label: Amount
        type: currency

    applied_at:
        label: Applied
        type: datetime

    voided_at:
        label: Voided
        type: datetime
        invisible: true
