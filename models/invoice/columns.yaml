# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID

    user_ip:
        label: IP Address
        invisible: true

    email:
        label: Email

    first_name:
        label: First name
        invisible: true

    last_name:
        label: Last name
        invisible: true

    processed_at:
        label: Processed at
        type: date
        invisible: true

    status_updated_at:
        label: Status updated
        type: date
        invisible: true

    sent_at:
        label: Date
        type: date

    due_at:
        label: Due date
        type: date

    total:
        label: Total

    subtotal:
        label: Subtotal
        invisible: true

    phone:
        label: Phone
        invisible: true

    company:
        label: Company
        invisible: true

    street_addr:
        label: Address
        invisible: true

    city:
        label: City
        invisible: true

    zip:
        label: Zip
        invisible: true

    state:
        label: State
        relation: state
        select: @name
        invisible: true

    country:
        label: Country
        relation: country
        select: @name
        invisible: true

    payment_type:
        label: Gateway
        relation: payment_type
        select: @name
        invisible: true
