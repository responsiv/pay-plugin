# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: responsiv.pay::lang.invoice_status_log.id
        invisible: true

    status:
        label: responsiv.pay::lang.invoice_status_log.status
        relation: status
        select: name
        sortable: false

    created_at:
        label: responsiv.pay::lang.invoice_status_log.created_at
        type: datetime

    comment:
        label: responsiv.pay::lang.invoice_status_log.comment
