# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        invisible: true

    status:
        label: Status
        relation: status
        select: name
        sortable: false

    created_at:
        label: Recorded
        type: datetime

    comment:
        label: Comment
