# ===================================
#  Form Field Definitions
# ===================================

fields:
    status:
        label: Status
        type: dropdown
        placeholder: -- select --

    comment:
        label: Comment
        type: textarea
        size: small

    send_notifications:
        label: Send Notifications
        type: checkbox
