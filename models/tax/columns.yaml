# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        invisible: true

    name:
        label: Name
        searchable: true

    code:
        label: API Code
        searchable: true

    description:
        label: Description
        searchable: true

    is_default:
        label: Default
        type: switch
