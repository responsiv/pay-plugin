# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: responsiv.pay::lang.tax.id
        invisible: true

    name:
        label: responsiv.pay::lang.tax.name

    description:
        label: responsiv.pay::lang.tax.description

    is_default:
        label: responsiv.pay::lang.tax.is_default
        type: switch