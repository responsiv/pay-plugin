# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        invisible: true

    name:
        label: Class Name

    description:
        label: Description

    is_default:
        label: Default
        type: switch