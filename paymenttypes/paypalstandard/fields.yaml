fields:
    test_mode:
        label: Sandbox mode
        type: switch
        tab: Configuration
        comment: Use the PayPal Sandbox Test Environment to try out Website Payments. You should be logged into the PayPal Sandbox to work in test mode. 

    business_email:
        label: Business email
        comment: PayPal business account email address.
        tab: Configuration

    pdt_token:
        label: PDF token
        comment: PayPal Payment Data Transfer token.
        tab: Configuration

    cancel_page:
        label: Cancel page
        comment: Page to which the user’s browser is redirected if payment is cancelled.
        tab: Configuration
        span: auto
        type: dropdown

    invoice_status:
        label: Invoice status
        comment: Select status to assign the invoice in case of successful payment.
        tab: Configuration
        span: auto
        type: relation


    invoice_status_id:
        label: Invoice Status ID
        context: hidden

